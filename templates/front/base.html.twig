<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <title>{% block metaTitle %}{% endblock metaTitle %}</title>
</head>
<body>
    <header class="header">
        <nav>
            <form action="{{ path('search') }}" method="get">
                <input type="text" name="q" placeholder="recherche...">
                <input type="submit" value="Rechercher">
            </form>

            {#
                https://symfony.com/doc/3.4/templating/embedding_controllers.html
                avec la syntaxe de : https://stackoverflow.com/questions/45396501/how-to-embed-controllers-in-a-template-using-symfony-4-directory-structure
            #}
            {{ render(controller('App\\Controller\\CategoryController::categoriesAll')) }}

{#            <ul class="menu-items">#}
{#                {% for category in categories.getAll() %}#}
{#                    <li class="menu-item"><a href="{{ path('categoryShow', {'id': category.id}) }}">{{ category.title }}</a></li>#}
{#                {% endfor %}#}
{#            </ul>#}
        </nav>
    </header>
        <main>
            {% block main %}
            {% endblock main %}
        </main>
    <footer class="footer">
    </footer>
</body>
</html>
